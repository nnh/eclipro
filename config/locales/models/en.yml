en:
  activerecord: &activerecord
    models:
      protocol: Protocol
    attributes:
      protocol:
        title: Title
        principal_investigator: Principal investigator
    enum:
      protocol:
        role:
          co_author: CoAuthor
          author: Author
          reviewer: Reviewer
    # errors:
