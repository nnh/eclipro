$('.new-comment-form').empty();
$('.reply-comment-form').empty();
$('#add-comment-form').addClass('hide');
$('#reply-<%= parent_id %>').html(
  '<%= escape_javascript(render "form", protocol: protocol, content: content, comment: comment, parent_id: parent_id ) %>');
